[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: Tests que requieren API keys reales (excluidos por defecto)
    unit: Tests unitarios sin dependencias externas

addopts = -v --tb=short -m "not integration"
